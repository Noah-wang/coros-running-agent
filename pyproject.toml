[project]
name = "coros-running-agent"
version = "0.1.0"
description = "Self-hosted COROS running coach agent."
requires-python = ">=3.13"
dependencies = [
    "apscheduler",
    "bilibili-api-python",
    "discord.py",
    "fitdecode>=0.11.0",
    "httpx",
    "langgraph>=1.2.11",
    "mcp",
    "numpy",
    "openai",
    "pypdf",
    "python-dotenv",
]

[tool.uv]
package = false
